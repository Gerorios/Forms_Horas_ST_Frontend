# Copiá este archivo a .env.local y ajustá la URL del backend si hace falta.
NEXT_PUBLIC_API_URL=http://localhost:3001

# Unificación ERP (Etapa 1) — módulo Certificaciones dentro de esta app.
# URL del FastAPI del Portal de Certificaciones (apiCert). En producción,
# alternativa same-origin vía proxy Nginx (/certapi/) — decisión al deployar.
NEXT_PUBLIC_CERT_API_URL=http://localhost:8000
